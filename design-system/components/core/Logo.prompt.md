The wordmark. Always pass `src` with the correct relative path to `assets/logo.png` for your page.

```jsx
<Logo height={34} src="../../assets/logo.png" />
```

There is no standalone icon-only variant in the supplied material — do not invent one.
