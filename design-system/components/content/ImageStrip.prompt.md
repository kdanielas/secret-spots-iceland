Photography carries the brand — use this between two text-heavy sections instead of adding more copy.

```jsx
<ImageStrip images={[a, b, c, d]} ratio="5 / 4" />
```

No captions, no overlays, no text on top.
