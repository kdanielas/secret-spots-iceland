Header. Nine text links, 14px Lato, no buttons in the bar — the CTA lives in the hero.

```jsx
<Navbar logoSrc="../../assets/logo.png" active="Home"
  items={[{ label: 'Home' }, { label: 'Tours' }, { label: 'About' }, { label: 'How it works' }, { label: 'FAQ' }]} />
```
